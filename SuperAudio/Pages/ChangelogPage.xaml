<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SuperAudio.Pages.ChangelogPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SuperAudio.Pages"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            x:Name="PageHeader"
            MaxWidth="1064"
            Margin="36,24,36,0"
            AutomationProperties.HeadingLevel="Level1"
            Style="{StaticResource TitleTextBlockStyle}"
            x:Uid="ChangelogPageTitle"
            Text="更新日志"/>
        <!-- 滚动列表 -->
        <ScrollViewer
            Grid.Row="1"
            VerticalScrollBarVisibility="Auto"
            MaxWidth="1064"
            Margin="36,24,36,0">
            <ItemsRepeater x:Name="ChangelogRepeater" ItemsSource="{x:Bind ChangelogData}">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate>
                        <!-- 每个版本的卡片 -->
                        <Border
                            Background="{ThemeResource CardBackgroundFillColorDefault}"
                            CornerRadius="8"
                            Margin="0,0,0,16"
                            Padding="16,14">
                            <StackPanel>
                                <!-- 版本标题 -->
                                <TextBlock
                                    Text="{Binding VersionDisplay}"
                                    Style="{StaticResource SubtitleTextBlockStyle}"
                                    FontWeight="SemiBold" />
                                <!-- 发布日期 -->
                                <TextBlock
                                    Text="{Binding Date}"
                                    Style="{StaticResource CaptionTextBlockStyle}"
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Margin="0,4,0,8" />
                                <!-- 功能列表 -->
                                <ItemsRepeater ItemsSource="{Binding Features}">
                                    <ItemsRepeater.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock 
                                            Text="{Binding}" 
                                            TextWrapping="WrapWholeWords" 
                                            FontSize="14" 
                                            IsTextSelectionEnabled="True"
                                            Margin="0,4" />
                                        </DataTemplate>
                                    </ItemsRepeater.ItemTemplate>
                                </ItemsRepeater>
                            </StackPanel>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>
    </Grid>
</Page>
