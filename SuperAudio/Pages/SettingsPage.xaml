<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="SuperAudio.Pages.SettingsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SuperAudio.Pages"
    xmlns:viewModel="using:SuperAudio.ViewModels"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:toolkit="using:CommunityToolkit.WinUI.Controls"
    mc:Ignorable="d">
    <Page.Resources>
        <!--  These styles can be referenced to create a consistent SettingsPage layout  -->
        <!--  Reference SettingsExpander Page in Windows Community Toolkit Gallery for the full sample  -->

        <!--  Spacing between cards  -->
        <x:Double x:Key="SettingsCardSpacing">4</x:Double>

        <!--  Style (inc. the correct spacing) of a section header  -->
        <Style
            x:Key="SettingsSectionHeaderTextBlockStyle"
            BasedOn="{StaticResource BodyStrongTextBlockStyle}"
            TargetType="TextBlock">
            <Style.Setters>
                <Setter Property="Margin" Value="1,30,0,6" />
            </Style.Setters>
        </Style>
    </Page.Resources>
    <Grid x:Name="RootGrid" Loaded="RootGrid_Loaded">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <TextBlock
            x:Name="PageHeader"
            MaxWidth="1064"
            Margin="36,24,36,0"
            AutomationProperties.HeadingLevel="Level1"
            Style="{StaticResource TitleTextBlockStyle}"
            Text="Settings" />
        <ScrollView
            Grid.Row="1"
            Padding="36,0,36,0"
            VerticalScrollBarVisibility="Auto"
            VerticalScrollMode="Auto">
            <Border>
                <StackPanel MaxWidth="1064" Spacing="{StaticResource SettingsCardSpacing}">
                    <StackPanel.ChildrenTransitions>
                        <RepositionThemeTransition />
                    </StackPanel.ChildrenTransitions>
                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="Appearance &amp; behavior" />
                    <toolkit:SettingsCard x:Uid="AppTheme">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE790;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <ComboBox
                            x:Name="themeMode" AutomationProperties.AutomationId="themeModeComboBox" ItemsSource="{x:Bind ViewModel.Themes}" SelectedItem="{x:Bind ViewModel.ThemeSelect,Mode=TwoWay}">
                        </ComboBox>
                    </toolkit:SettingsCard>
                    <toolkit:SettingsCard x:Uid="Language">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE775;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <StackPanel Orientation="Horizontal">
                            <ComboBox x:Name="language"  ItemsSource="{x:Bind ViewModel.Languages,Mode=TwoWay}" SelectedItem="{x:Bind ViewModel.LanageSelect,Mode=TwoWay}"/>
                        </StackPanel>
                    </toolkit:SettingsCard>
                    <toolkit:SettingsCard Header="Navigation style" x:Uid="NavigationStyle">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xF594;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <ComboBox x:Name="navigationLocation" ItemsSource="{x:Bind ViewModel.NavStyles}" SelectedItem="{x:Bind ViewModel.NavStyleSelect,Mode=TwoWay}">
                        </ComboBox>
                    </toolkit:SettingsCard>

                    <toolkit:SettingsExpander Description="Controls provide audible feedback" Header="Sound">
                        <toolkit:SettingsExpander.HeaderIcon>
                            <FontIcon Glyph="&#xEC4F;" />
                        </toolkit:SettingsExpander.HeaderIcon>
                        <ToggleSwitch x:Name="soundToggle" Toggled="soundToggle_Toggled" />
                        <toolkit:SettingsExpander.Items>
                            <toolkit:SettingsCard
                                x:Name="SpatialAudioCard"
                                Header="Enable Spatial Audio"
                                IsEnabled="False">
                                <toolkit:SettingsCard.Description>
                                    <HyperlinkButton Click="soundPageHyperlink_Click" Content="Learn more about enabling sounds in your app" />
                                </toolkit:SettingsCard.Description>
                                <ToggleSwitch x:Name="spatialSoundBox" Toggled="spatialSoundBox_Toggled" />
                            </toolkit:SettingsCard>
                        </toolkit:SettingsExpander.Items>
                    </toolkit:SettingsExpander>

                    <toolkit:SettingsCard Description="Clear your recent or favorite samples" Header="Manage samples">
                        <toolkit:SettingsCard.HeaderIcon>
                            <FontIcon Glyph="&#xE8A9;" />
                        </toolkit:SettingsCard.HeaderIcon>
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <Button
                                x:Name="ClearRecentBtn"
                                MinWidth="120"
                                Click="ClearRecentBtn_Click"
                                Content="Clear recents" />
                            <Button
                                x:Name="UnfavoriteBtn"
                                MinWidth="120"
                                Click="UnfavoriteBtn_Click"
                                Content="Remove favorites" />
                        </StackPanel>
                    </toolkit:SettingsCard>


                    <!--  About  -->
                    <TextBlock Style="{StaticResource SettingsSectionHeaderTextBlockStyle}" Text="关于" />
                    <toolkit:SettingsExpander
                        Margin="0,0,0,36"
                        Description="© 2026 顾留中 All rights reserved."
                        Header="{x:Bind ViewModel.Title}">
                        <toolkit:SettingsExpander.HeaderIcon>
                            <BitmapIcon ShowAsMonochrome="False" UriSource="/Assets/StoreLogo.scale-100.png" />
                        </toolkit:SettingsExpander.HeaderIcon>
                        <TextBlock
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            IsTextSelectionEnabled="True"
                            Text="{x:Bind ViewModel.Version}" />
                        <toolkit:SettingsExpander.Items>
                            <toolkit:SettingsCard
                                x:Name="bugRequestCard"
                                Command="{x:Bind ViewModel.ToBugReportCommand}"
                                Header="File a bug or request new sample"
                                IsClickEnabled="True">
                                <toolkit:SettingsCard.ActionIcon>
                                    <FontIcon Glyph="&#xE8A7;" />
                                </toolkit:SettingsCard.ActionIcon>
                            </toolkit:SettingsCard>
                            <toolkit:SettingsCard
                                HorizontalContentAlignment="Left"
                                ContentAlignment="Vertical"
                                Header="Dependencies &amp; references">
                                <StackPanel Margin="-12,0,0,0" Orientation="Vertical">
                                    <HyperlinkButton Content="WinUI 3" NavigateUri="https://aka.ms/winui" />
                                    <HyperlinkButton Content="Windows Community Toolkit" NavigateUri="https://aka.ms/toolkit/windows" />
                                </StackPanel>
                            </toolkit:SettingsCard>
                        </toolkit:SettingsExpander.Items>
                    </toolkit:SettingsExpander>
                </StackPanel>
            </Border>
        </ScrollView>
    </Grid>
</Page>
